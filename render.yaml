services:
  - type: web
    name: video-stream-backend
    env: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
      - key: CORS_ORIGINS
        value: https://video-stream-frontend-lexb.onrender.com

  - type: web
    name: video-stream-frontend
    env: static
    rootDir: frontend
    buildCommand: |
      npm install
      npm run build
    staticPublishPath: ./dist
    envVars:
      - key: VITE_WS_URL
        value: https://video-stream-backend-jr2c.onrender.com 